<div id="recipe-ingredients-list-header">
  <h2>Ingredients</h2>
</div>

<div id="recipe-ingredients-list">
  <ul>
	<% @recipe.ingredients.each do |ingredient| %>
		<li>
    <% #At some point we should add ingredient qtys here %>
    <span style="color:<%=recipe_ingredient_in_pantry(ingredient)%>"><%= ingredient %></span>
    </li>
	<% end %>
  </ul>
</div>
